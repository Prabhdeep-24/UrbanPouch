<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      background-color: #f4f6fb;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    .account-wrapper {
      flex: 1;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 40px 15px;
    }

    .account-card {
      background: white;
      border-radius: 16px;
      padding: 40px;
      max-width: 420px;
      width: 100%;
      box-shadow: 0 5px 25px rgba(0, 0, 0, 0.1);
      text-align: center;
      animation: fadeIn 0.4s ease-in-out;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .account-avatar {
      width: 90px;
      height: 90px;
      border-radius: 50%;
      background: #007bff1a;
      display: flex;
      justify-content: center;
      align-items: center;
      font-size: 36px;
      color: #007bff;
      margin: 0 auto 20px;
    }

    .btn-primary {
      background-color: #007bff;
      border: none;
    }
    .btn-primary:hover {
      background-color: #0056b3;
    }

    .btn-danger {
      border: none;
    }

    .role-badge {
      background: #eaf3ff;
      color: #007bff;
      border-radius: 8px;
      padding: 5px 12px;
      font-size: 0.9rem;
      font-weight: 500;
      margin-top: 10px;
      display: inline-block;
    }

    .navbar .btn {
        transition: transform 0.15s ease;
    }
    .navbar .btn:hover {
        transform: scale(1.05);
    }

  </style>
</head>
<body>

 <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container">
    <a class="navbar-brand fw-bold" href="/">UrbanPouch</a>
    <div class="d-flex align-items-center gap-3">
          <!-- 👤 Regular User View -->
          <a href="/cart" class="btn btn-light position-relative">🛒 Cart</a>
          <a href="/users/account" class="btn btn-outline-light">My Account</a>
          <a href="/users/logout" class="btn btn-warning text-dark fw-semibold">Logout</a>
    </div>
  </div>
</nav>



  <div class="account-wrapper">
    <div class="account-card">
      <div class="account-avatar">
        <i class="bi bi-person"></i> <!-- Optional icon if Bootstrap Icons are included -->
        <%= user.Name.charAt(0).toUpperCase() %>
      </div>

      <h2 class="fw-bold mb-2">Hello, <%= user.Name.split(' ')[0] %> 👋</h2>
      <p class="text-muted mb-1">Email: <%= user.email %></p>
      <div class="role-badge">
        Account Type: <%= user.isAdmin ? "Admin" : "User" %>
      </div>

      <div class="mt-4">
        <a href="/cart" class="btn btn-primary w-100 mb-3">🛒 My Cart</a>
        <a href="/users/logout" class="btn btn-danger w-100">Logout</a>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
